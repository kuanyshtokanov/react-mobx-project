# react-mobx-project
this was a test project for job application to POWr

For development run => 
npm run development
open http://localhost:3000

For production run =>
npm start
open http://localhost:3000
